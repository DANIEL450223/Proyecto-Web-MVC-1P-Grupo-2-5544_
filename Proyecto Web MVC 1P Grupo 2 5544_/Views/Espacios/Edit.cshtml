@model Proyecto_Web_MVC_1P_Grupo_2_5544_.Models.Espacios

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Espacios</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="espacioId" />
            <div class="form-group form-check">
                <label class="form-check-label">
                    <input class="form-check-input" asp-for="estadoEspacio" /> @Html.DisplayNameFor(model => model.estadoEspacio)
                </label>
            </div>
            <div class="form-group">
                <label asp-for="vehiculoId" class="control-label"></label>
                <select asp-for="vehiculoId" class="form-control" asp-items="ViewBag.vehiculoId"></select>
                <span asp-validation-for="vehiculoId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
